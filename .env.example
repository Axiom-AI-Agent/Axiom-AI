# LLM — chat (GPT-4o-mini), merge (Gemini), router/guardrail (Groq)
OPENAI_API_KEY=
GOOGLE_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=

# Shared Supabase — same project for AI backend + dashboard team
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_DB_URL=

# Qdrant (Phase 4 — RAG)
QDRANT_URL=
QDRANT_API_KEY=

# Twilio WhatsApp Sandbox (Phase 1)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
MESSAGING_DRY_RUN=true

# Google Drive service account JSON path (Phase 4)
GOOGLE_SERVICE_ACCOUNT_JSON=

# Langfuse — tracing + prompt management
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PROMPT_LABEL=production

# Local dev fallback tenant (never use in production paths)
DEV_TENANT_ID=tenant-demo-physics

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000
# ── version control & editor ────────────────────────────────────────
.git
.gitignore
.gitattributes
.vscode
.idea
*.swp

# ── agent / graph tooling (not needed in images) ────────────────────
.claude
.cursor
.agents
graphify-out

# ── python build / venv / cache ─────────────────────────────────────
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.mypy_cache
.pytest_cache
.ruff_cache
.coverage
htmlcov

# ── demo UI (not part of API image) ─────────────────────────────────
demo-ui-org

# ── secrets — never bake into images ───────────────────────────────
.env
.env.local
.env.*.local

# ── large local data / scratch ─────────────────────────────────────
data/
docs/
scripts/
sql/
tests/
*.log
*.jsonl

# ── OS junk ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

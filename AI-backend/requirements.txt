# Axiom AI — Python 3.10+ recommended (3.11 preferred for MCP subprocess servers).
# On Python 3.9: pip install succeeds; MCP packages are skipped (use AGENT_USE_MCP=false
# or rely on in-process tool fallback in src/agents/runtime.py).

# LLM and Agent Framework (Phase 2+)
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# MCP — only on Python 3.10+ (FastMCP + langchain-mcp-adapters)
mcp>=1.28.0,<2; python_version >= "3.10"
langchain-mcp-adapters>=0.3.0,<0.4; python_version >= "3.10"

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
supabase>=2.0.0
qdrant-client>=1.12.0
psycopg[binary]>=3.2.0

# Twilio (Phase 1)
twilio>=9.0.0

# Voice transcription — audio duration measurement
mutagen>=1.47.0

langchain-google-genai>=2.0.0

# Observability
langfuse>=2.0.0
loguru>=0.7.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
httpx>=0.27.0

# Ingest — multi-format extraction (PDF/DOCX/MD) + structure-aware chunking
# pymupdf4llm is AGPL-3.0; compatible with this project's GPL-3.0 licence.
# A commercial relicence would need a licence from Artifex.
pymupdf4llm==1.28.2
mammoth==1.12.1
markdownify==1.2.3
tiktoken==0.13.0
langchain-text-splitters>=0.3.0

# Google Drive (Phase 4)
google-api-python-client>=2.100.0
google-auth>=2.0.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.5.0
